export default function AccountModel({ FirstName, LastName, Username, Password }) { 
    const fisrName = FirstName
    const lastName = LastName
    const username = Username
    const password = Password
}